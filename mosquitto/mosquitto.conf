# Allow anonymous access locally (or secure with username/password if needed)
allow_anonymous true

# WebSocket listener (for frontend)
listener 9001
protocol websockets

# TCP listener (for Python or IoT devices)
listener 1883
protocol mqtt
